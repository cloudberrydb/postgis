=Introduction

This module contains support for trajectory data modelling, which considers a sequence of spatio-temporal records as a basic data tuple instead a set of them. 

=Requirements

  Many functions are implemented with postgis.

=Building
  To build the topology support:

  $ make

=Testing

  To run regression tests:

  $ make check

=Install

To enable trajectory support:

  $ psql -f trajectory.sql <your_postgis_enabled_db>
  It will create a 'trajectory' schema in <your_postgis_enabled_db> hosting all the trajectory functions. To be run with db-owner perms. Remember to grant execute to users.

To uninstall:

    $ psql -c 'drop schema trajectory cascade'

=Usage

Example:
    SELECT trajectory.CreateTrajectory(<name>, [srid], [tolerance]);
   SELECT trajectory.DropTrajectory(<name>);


=Update Log
  v0.1 Create by Kuien Liu <kliu@pivotal.io>,  23 Sep, 2015

